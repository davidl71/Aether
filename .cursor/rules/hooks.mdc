---
description: Git hooks and automation hooks (pre-commit, exarp-go setup_hooks, Cursor session hooks)
alwaysApply: false
globs: ".pre-commit-config.yaml,scripts/setup_pre_commit_hook.sh,.cursor/hooks/*,scripts/git-hooks/*"
---

# Hooks

## Cursor session hooks (.cursor/hooks/)

- **session-prime.sh:** Runs when a new Composer conversation starts (sessionStart). Calls exarp-go session prime for this project via `scripts/run_exarp_go.sh` and returns `additional_context` (task summary, suggested next, handoff) so the AI has context without being asked. See `.cursor/hooks/README.md`.
- **Requires:** exarp-go (PATH or EXARP_GO_ROOT), optional `jq`. Cursor must have session-start / context hooks enabled (see Cursor docs).
- **After bulk task changes:** If you use scripts or CLI to mass-update Todo2 (e.g. all Review→Done), run `./scripts/run_exarp_go.sh task sync` so `.todo2/state.todo2.json` matches SQLite (see `.cursor/skills/aether-todo2-exarp/SKILL.md`).

## Pre-commit (framework)

- **Config:** `.pre-commit-config.yaml`
- **Install:** `pre-commit install` (in repo root)
- **Runs on:** `git commit` (on staged files)
- **Hooks:** trailing-whitespace, end-of-file-fixer, check-yaml, check-toml, mixed-line-ending, shfmt, shellcheck. **lint-staged** (local): runs `scripts/lint_staged.sh` to ruff-check and ruff-format **only staged .py files** (lint generated/edited files at commit time). Markdownlint is **not** in pre-commit (doc edits don’t block commits; use CI or `npm run lint:docs`).

See `docs/LINT_AND_AUTOMATION_MATRIX.md` for how pre-commit relates to `run_linters.sh` and CI.

## Optional: documentation validation hook

- **Script:** `scripts/setup_pre_commit_hook.sh`
- **Effect:** Installs a custom `.git/hooks/pre-commit` that validates docs when `docs/API_DOCUMENTATION_INDEX.md` is staged (format + link checks). Non-blocking link warnings; use `git commit --no-verify` to skip.
- **Note:** If you use the pre-commit framework (`pre-commit install`), this script’s hook may conflict; prefer one or merge logic into a single pre-commit hook.

## exarp-go: setup_hooks

- **Tool:** exarp-go MCP `setup_hooks` (action: git or patterns)
- **Use:** Install git hooks or automation patterns for exarp-go (e.g. task alignment, docs health). Call via Cursor with `workingDirectory` set to this project’s root.
- **Docs:** `.cursor/rules/project-automation.mdc`. See also `docs/EXARP_GO_GIT_HOOKS_LEARNINGS.md`, `docs/MCP_REQUIRED_SERVERS.md`.

## Summary

| Hook type | Config / script | Install / trigger |
|-----------|-----------------|-------------------|
| Cursor sessionStart | `.cursor/hooks/session-prime.sh` | Automatic when Composer starts (if Cursor hooks enabled) |
| Pre-commit (format, shell, **lint-staged**) | `.pre-commit-config.yaml` | `pre-commit install` |
| Versioned hooks (Co-authored-by, etc.) | `scripts/git-hooks/` | `git config core.hooksPath scripts/git-hooks` |
| Docs validation (optional) | `scripts/setup_pre_commit_hook.sh` | Run script once |
| exarp-go (git/patterns) | exarp-go MCP | Use `setup_hooks` tool in Cursor |

**Build / proto:** C++ from `proto/messages.proto` is generated at build by CMake. For quiet + JSON build result use `build:ai-friendly` or `./scripts/build_ai_friendly.sh` (see `.cursor/rules/just-cmake-shortcuts.mdc`).

See also: `docs/LINT_AND_AUTOMATION_MATRIX.md`, `docs/archive/AI_EDITOR_SETUP.md` (Hooks section).
