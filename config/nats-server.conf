# NATS Server Configuration for Development
# This configuration is optimized for local development

# Server port (default: 4222)
port: 4222

# HTTP monitoring port (default: 8222)
http_port: 8222

# Host (bind to all interfaces for development)
host: "0.0.0.0"

# Logging
debug: false
trace: false
logtime: true
log_file: "logs/nats-server.log"
log_size_limit: 100MB
max_log_file: 10

# Connection limits
max_connections: 1000
max_payload: 1048576  # 1MB

# Ping interval (seconds)
ping_interval: 20
ping_max: 2

# Write deadline (seconds)
write_deadline: 10

# Cluster configuration (disabled for development)
# Uncomment for clustering:
# cluster {
#   port: 6222
#   routes = [
#     "nats://localhost:6222"
#   ]
# }

# JetStream (disabled for Phase 1 - enable later if needed)
# jetstream {
#   store_dir: "logs/jetstream"
# }

# Authentication (disabled for development)
# Uncomment for production:
# authorization {
#   users = [
#     { user: "backend", password: "$2a$..." }
#   ]
# }

# TLS (disabled for development)
# Uncomment for production:
# tls {
#   cert_file: "certs/server-cert.pem"
#   key_file: "certs/server-key.pem"
# }
