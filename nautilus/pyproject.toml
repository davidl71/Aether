[project]
name = "nautilus-ibkr"
version = "0.1.0"
description = "NautilusTrader IBKR integration"
requires-python = ">=3.11"
dependencies = [
    "nautilus_trader[ib]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
