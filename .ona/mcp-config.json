{
  "mcpServers": {
    "semgrep": {
      "name": "semgrep",
      "command": "npx",
      "args": [
        "-y",
        "@semgrep/mcp-server-semgrep"
      ],
      "disabled": false,
      "timeout": 30,
      "description": "Semgrep security scanning for code analysis - checks for security vulnerabilities, bugs, and code quality issues"
    },
    "filesystem": {
      "name": "filesystem",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceFolder}"
      ],
      "disabled": false,
      "timeout": 30,
      "description": "File system operations for reading, writing, and managing project files",
      "workingDir": "${workspaceFolder}"
    },
    "git": {
      "name": "git",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-git",
        "--repository",
        "${workspaceFolder}"
      ],
      "disabled": false,
      "timeout": 30,
      "description": "Git version control operations - AI can understand git history, help with commit messages, and understand branch structure and diffs"
    },
    "context7": {
      "name": "context7",
      "command": "npx",
      "args": [
        "-y",
        "@upstash/context7-mcp"
      ],
      "disabled": false,
      "timeout": 30,
      "description": "Context7 MCP server - provides up-to-date, version-specific documentation and code examples. Access current documentation by appending 'use context7' to prompts."
    },
    "notebooklm": {
      "name": "notebooklm",
      "command": "npx",
      "args": [
        "-y",
        "notebooklm-mcp@latest"
      ],
      "disabled": false,
      "timeout": 60,
      "description": "NotebookLM MCP server for summarizing YouTube videos, documentation links, and creating zero-hallucination knowledge base"
    },
    "tractatus_thinking": {
      "name": "tractatus_thinking",
      "command": "npx",
      "args": [
        "-y",
        "tractatus-thinking"
      ],
      "disabled": false,
      "timeout": 30,
      "description": "Tractatus Thinking MCP server for logical concept analysis and structured thinking - breaks down complex concepts into atomic truths, reveals multiplicative relationships, and finds missing elements"
    },
    "sequential_thinking": {
      "name": "sequential_thinking",
      "command": "python3",
      "args": [
        "-m",
        "sequential_thinking"
      ],
      "disabled": false,
      "timeout": 30,
      "description": "Sequential Thinking MCP server for structured problem-solving and implementation workflow - converts structural understanding from Tractatus Thinking into actionable implementation steps",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/python:${workspaceFolder}"
      }
    }
  },
  "globalTimeout": 45,
  "logLevel": "info"
}
