{
  "mcpServers": {
    "exarp-go": {
      "command": "{{PROJECT_ROOT}}/../../mcp/exarp-go/scripts/run_exarp_go.sh",
      "args": [],
      "env": {
        "PROJECT_ROOT": "{{PROJECT_ROOT}}",
        "EXARP_WATCH": "0"
      },
      "description": "exarp-go MCP: docs health, task alignment, duplicates, security. Set workingDirectory to project root. Use installed exarp-go (PATH or EXARP_GO_ROOT)."
    }
  }
}
