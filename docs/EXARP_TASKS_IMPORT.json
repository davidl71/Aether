{
  "tasks": [
    {
      "name": "Execute script and CMake refactor verification",
      "priority": "medium",
      "tags": "verification,scripts,cmake,automation",
      "long_description": "🎯 **Objective:** Verify the recent shell-script deduplication and CMake-preset refactor, then optionally execute remaining SCRIPTS_AUDIT items.\n\n📋 **Acceptance criteria:**\n- Run `./scripts/build_fast.sh` on at least one platform (macOS or Linux); build succeeds or fails with a clear preset error.\n- Run `./scripts/build_distributed.sh` on at least one platform; build uses preset and `cmake --build` (no `make -C`).\n- Run `./scripts/test_repo_install.sh` (dry-run) and `sudo ./scripts/test_repo_install.sh --install` in a safe env if applicable.\n- Confirm `release_x86.sh` uses `cmake --preset macos-x86_64-release` (already done; spot-check only).\n- Document outcome in a result comment (pass/fail, platform, any fixes made).\n\n🚫 **Scope:** Verification of build_fast.sh, build_distributed.sh, test_repo_install.sh, release_x86.sh; optional SCRIPTS_AUDIT §5/§6.3. Excluded: full Unite items; exarp-go migration.\n\n🔧 **Technical:** Presets: *-release-sccache, *-release-ccache, *-release-distcc (CMakePresets.json). Ref: scripts/SCRIPTS_AUDIT.md §5–6.\n\n📁 **Files:** scripts/build_fast.sh, scripts/build_distributed.sh, scripts/test_repo_install.sh, scripts/release_x86.sh, CMakePresets.json, scripts/SCRIPTS_AUDIT.md.\n\n🧪 **Verification:** Build/test commands above; ctest after build if desired."
    }
  ],
  "_comment": "Create this task via: (1) Cursor chat with exarp-go MCP connected: 'Create todo: Execute script and CMake refactor verification' and paste long_description from docs/DRAFT_EXECUTE_TASK.md Option A. (2) Or use task_workflow create with name + long_description + tags verification,scripts,cmake,automation + priority medium."
}
