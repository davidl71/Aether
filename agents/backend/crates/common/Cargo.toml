[package]
name = "common"
edition = "2021"
version.workspace = true
description = "Shared utilities (expiry parsing, snapshot types)"

[dependencies]
chrono = { workspace = true }
derive_builder = { workspace = true }
serde = { workspace = true }
