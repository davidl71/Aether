List current Todo2 tasks via the exarp-go task_workflow MCP tool.

Call `exarp-go-task_workflow` with `action=sync`, `sub_action=list`.

If $ARGUMENTS is provided, filter by that status (e.g. "Todo", "In Progress").

Display results as a readable table with ID, Priority, Status, and Name.
