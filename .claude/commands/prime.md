---
description: Prime the session with project context (tasks, hints, handoffs)
---

Call the exarp-go `session` MCP tool with `action=prime`, `include_hints=true`, `include_tasks=true`.

Summarize the result: show current focus task, suggested next tasks, task counts, and any hints or handoffs from previous sessions.
