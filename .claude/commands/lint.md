---
description: Run all linters (cppcheck, clang-analyze, eslint, bandit, etc.)
---

Run the full linter suite:

```bash
cd /Users/dlowes/Projects/ib_box_spread_full_universal && just lint 2>&1
```

Report: which linters ran, any warnings or errors found, and suggested fixes.
