---
- name: Project-specific setup (ib_box_spread)
  hosts: localhost
  connection: local
  gather_facts: yes
  vars:
    ibjts_dir: "{{ lookup('env','IBJTS_DIR') | default('', true) }}"
  roles:
    - role: ib_box_spread
      tags: ["project", "ib", "cmake", "deps"]
